# 发布约定

## 桌面端

- 用户说“打包更新”：
  含义是“按发布流程打包，并发布到 GitHub Release”，不提交源码仓库内容。
- 用户说“本地更新”：
  含义是“只打包一个本地安装版本”，不发布 GitHub Release，也不提交源码仓库内容。
- 除非用户额外明确说明，否则默认都按以上约定执行。

## 移动端

- 移动端首版按 `Vercel + H5/PWA` 发布。
- 桌面端和移动端是两条发布链路：
  - 桌面端：继续按安装包 / GitHub Release 更新。
  - 移动端：更新线上页面通常需要同步源码到 GitHub，由 Vercel 重新部署。
- 如果只是“打包更新”或“本地更新”，默认只影响桌面端，不自动更新移动端站点。
- 如果用户说“更新移动端”，默认理解为：
  - 相关源码需要同步到 GitHub，触发 Vercel 重新部署；
  - 或者用户明确要求手动部署 Vercel 静态产物。
